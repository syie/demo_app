<h1>Listing microposters</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @microposters.each do |microposter| %>
  <tr>
    <td><%= microposter.content %></td>
    <td><%= microposter.user_id %></td>
    <td><%= link_to 'Show', microposter %></td>
    <td><%= link_to 'Edit', edit_microposter_path(microposter) %></td>
    <td><%= link_to 'Destroy', microposter, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Microposter', new_microposter_path %>
